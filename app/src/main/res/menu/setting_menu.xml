<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_descrip_search"
        android:icon="@drawable/ic_magnify_white_48dp"
        android:title="Tìm Kiếm"
        app:showAsAction="always"
        app:actionViewClass="android.support.v7.widget.SearchView" />
    <item
        android:id="@+id/action_descrip_update"
        android:icon="@drawable/ic_main_updates"
        android:title="Tải Lại"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_descrip_share"
        android:icon="@drawable/ic_share_variant_white_36dp"
        android:title="Chia Sẻ"
        app:showAsAction="collapseActionView"/>

    <!-- Settings, should always be in the overflow
    <item android:id="@+id/action_settings"
        android:title="Whatever"
        app:showAsAction="never"/>-->
</menu>

